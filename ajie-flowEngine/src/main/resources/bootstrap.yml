server:
   port: 8002
spring:
   application:
      name: flowEngine
   cloud:
      config:
         label: main
         profile: dev
         discovery:
            enabled: true
            serviceId: config-server
eureka:
   client:
      serviceUrl:
         defaultZone: http://localhost:8000/eureka/