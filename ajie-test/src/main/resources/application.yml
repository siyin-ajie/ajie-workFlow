server:
  port: 8201
 
spring:
  application:
    name: ajie-test
 
#注册中心指向start    
eureka:
  instance:
    instance-id: ajie-test
    appname: ${spring.application.name}
  client: 
    service-url: 
      defaultZone: http://localhost:8888/eureka/